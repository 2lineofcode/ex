name: ex
description: Powerfull Ex-Material UI (Material UI improvement) |  all material widget with functional many helper/utils like `ExHttp, ExLog, ExAlert, ExBottomSheet, ExDialog, ExSnackbar` 
version: 0.2.1
homepage: https://github.com/abehbatre/ex

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cached_network_image: ^3.2.0
  flutter:
    sdk: flutter
  flutter_svg: ^1.0.3
  get: ^4.6.1
  http: ^0.13.5
  intl: ^0.17.0
  photo_view: ^0.14.0
  velocity_x: ^3.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/